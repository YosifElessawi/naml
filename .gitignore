__pycache__/
*.py[cod]
*.egg-info/
.DS_Store
.venv/
.idea/
.vscode/

# Local-only overrides
.agents-orchestrator.local.toml

# Nested playground repo — its own git history lives at GitHub.
/mock-target/

# Web tier build artifacts.  Pattern with and without trailing slash so
# the [worktree] symlink (a file-typed symlink pointing at a directory)
# is also ignored — slash-only patterns match directories.
web/node_modules
web/node_modules/
web/dist/
web/.vite/
